# Public facing API
# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String!,
    description: String,
    price: Int!,
    image: String,
    largeImage: String
  ) : Item!
}

type Dog {
  name: String!
}

type Query {
  dogs: [Dog]!
  items: [Item]!
}